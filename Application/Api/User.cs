namespace Application.Api.Components
{

//include_once './app/helper/strings.php';

//use \app\helper\strings as strings;

public static class user
{
//  public function __construct()
//  { }
//  public function findUserByData($Firstname, $Lastname, $Email)
//  {
//    global $app;
//    $lstrQuery = "SELECT mea.email, mp.active, mp.id as person_id, mp.firstname, mp.lastname FROM misc_email_addresses mea, misc_mapping_emails mme, misc_person mp WHERE mea.email = '" . $Email . "' AND mea.active = 1 AND mp.active = 1 AND mea.id = mme.email_id AND mme.type = 'person' AND mme.mapping_id = mp.id";
//    $larrReturnData = $app->DB->execute($lstrQuery);
//    if ($larrReturnData) {
//      //Suche, ob der Benutzer eine Anmeldung besitzt
//    } else {
//      //Lege Benutzer an

//    }
//    var_dump($larrReturnData);
//    //Wenn Benutzer identifiziert, liefere ID zurück
//    //Wenn Benutzer nicht identifiziert, lege an und liefere ID zurück
//  }
//  public function newUser($UserName, $UserMail) {

//  }
}
	
}